package cd.genuc.model;

/**
 * Statuts possibles pour une inscription.
 * Utilisé dans les entités, repositories, services et contrôleurs.
 */
public enum StatutInscription {
    EN_ATTENTE,
    VALIDE,
    REJETE
}